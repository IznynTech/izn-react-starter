/**
 * components/Dialog/styles.js
 */
import { makeStyles } from '@material-ui/core/styles';
const config = {
  paper: `
    min-width: 500px;
  `,
};

export default makeStyles(config, { name: 'MuiDialog' });
